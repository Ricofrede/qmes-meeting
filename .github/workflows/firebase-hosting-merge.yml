# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master

env:
  VITE_ENV: ${{secrets.VITE_ENV}}
  VITE_FB_API_KEY: ${{secrets.VITE_FB_API_KEY}}
  VITE_FB_AUTH_DOMAIN: ${{secrets.VITE_FB_AUTH_DOMAIN}}
  VITE_FB_PROJECT_ID: ${{secrets.VITE_FB_PROJECT_ID}}
  VITE_FB_STORAGE_BUCKET: ${{secrets.VITE_FB_STORAGE_BUCKET}}
  VITE_FB_MESSAGING_SENDER_ID: ${{secrets.VITE_FB_MESSAGING_SENDER_ID}}
  VITE_FB_APP_ID: ${{secrets.VITE_FB_APP_ID}}
  VITE_FB_MEASURE_ID: ${{secrets.VITE_FB_MEASURE_ID}}
  VITE_EJS_SERVICE_ID: ${{secrets.VITE_EJS_SERVICE_ID}}
  VITE_EJS_TEMPLATE_ID: ${{secrets.VITE_EJS_TEMPLATE_ID}}
  VITE_EJS_USER_ID: ${{secrets.VITE_EJS_USER_ID}}
  VITE_ADMIN_URL: ${{secrets.VITE_ADMIN_URL}}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd client && yarn install && yarn build
      - run: cd admin && yarn install && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_QMES_MEETING }}'
          channelId: live
          projectId: qmes-meeting
